- name: ESXi Installation Final
  hosts: klssyman01
  gather_facts: no
  vars_files:
    - servers.yml
    - defaultvars.yml

  tasks:
    - name: Mount Files and Reboot
      include_tasks: bmc_tasks.yml
      loop: "{{ servers }}"
      loop_control:
        loop_var: server