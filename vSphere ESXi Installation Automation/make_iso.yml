- name: Prepare VMware ISO with custom Kickstart desires
  hosts: ansibleserver_hostname
  gather_facts: false
  vars_files:
    - servers.yml
    - defaultvars.yml

  tasks:
    - name: Process each server
      include_tasks: iso_tasks.yml
      loop: "{{ servers }}"
      loop_control:
        loop_var: server
