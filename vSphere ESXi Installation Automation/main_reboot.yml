- name: Bulk Reboot
  hosts: klssyman01
  gather_facts: false
  vars_files:
    - servers.yml
    - defaultvars.yml

  tasks:
    - name: Reboot process each server
      include_tasks: force_reboot.yml
      loop: "{{ servers }}"
      loop_control:
        loop_var: server